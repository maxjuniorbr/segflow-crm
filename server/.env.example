# Porta do servidor
PORT=3001

# URL do banco de dados PostgreSQL (pode usar SQLite para dev)
DATABASE_URL=postgres://postgres:senha@localhost:5432/segflow_crm

# Segredo para JWT
JWT_SECRET=seu_segredo_super_secreto

# Ambiente (development ou production)
NODE_ENV=development
